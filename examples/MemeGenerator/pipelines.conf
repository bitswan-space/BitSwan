[deployment]
pre=internal/memegenerator
# NOTE: To build custom Pipeline runtime environment (PRE), just build the docker image yourself. 
# Use code in Dockerfile and name image as internal/memegenerator . `docker build -t internal/memegenerator` should do it :)

expose=true
